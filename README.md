# FamBoard Base (no sync)
Kis, offline-alap (nincs Firebase/Sync). Helyben (localStorage) tárolja az adatokat.

## Indítás
```bash
npm i
npm run dev
```
Build:
```bash
npm run build
```
A `dist/` mappa publikálható GitHub Pages-re.
